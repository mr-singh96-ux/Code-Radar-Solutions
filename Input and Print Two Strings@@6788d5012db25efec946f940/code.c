#include <stdio.h>
#include <string.h>

int main() {
    char str1[10], str2[10];
    scanf("%s", &str1);
    scanf("%s", &str2);
    printf("You entered: %s and %s",str1,str2);
    return 0;
}