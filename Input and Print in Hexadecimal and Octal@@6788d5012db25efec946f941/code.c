#include <stdio.h>
#include <string.h>

int main() {
    
    int a,b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("Hexadecimal: %x\n",toupper(a));
    printf("Octal: %X",b);
    return 0;
}