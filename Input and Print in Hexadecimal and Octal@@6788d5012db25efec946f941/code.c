#include <stdio.h>
#include <string.h>

int main() {
    
    int a,b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("Hexadecimal: %X\n",toupper(a));
    printf("Octal: %o",b);
    return 0;
}